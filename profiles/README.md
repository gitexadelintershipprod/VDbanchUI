# Profiles

The application creates default JSON profiles on first launch:

- `Default-4K-Random-Read`
- `Default-70-30-Random-Mix`
- `Default-Filesystem-Random-Read`

User-created profiles are also stored in this directory.
